version: '1.0'
steps:
  build_image:
    type: build
    title: Build codefresh-go
    image-name: ${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}
    tag: test
    target: test
  perform_tests:
    title: Perform unit tests
    image: ${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}:test
    working_directory: ${{main_clone}}
    commands:
    - go test -v ./...
